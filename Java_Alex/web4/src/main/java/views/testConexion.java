package views;
import Models.Conexion;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.logging.Level;
import java.util.logging.Logger;

public class testConexion {
    public static void main(String[] args) {
    	/*try {
    		Conexion C = new Conexion();
    		
    		String sentencia_sql = "select * from usuarios";
    		
    		PreparedStatement psmt = C.getConexion().prepareStatement(sentencia_sql);
    		ResultSet rs = psmt.executeQuery();
    		
    		while(rs.next()) {
    			System.out.println("Usuario: " + rs.getString(5));
    		}
    	} catch (SQLException ex) {
    		Logger.getLogger(testConexion.class.getName()).log(Level.SEVERE, null, ex); 
    		
    	}*/
    }
    }


